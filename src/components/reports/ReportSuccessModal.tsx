import Card from '../common/Card';

export default function ReportSuccessModal() {
  return <Card className="border-primary bg-green-light text-green-dark">Report saved locally. Supabase persistence is ready to wire next.</Card>;
}
